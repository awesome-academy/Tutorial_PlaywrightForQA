# Tutorial_playwright
# Chạy tất cả (bao gồm cả setup)
npx playwright test

# Chỉ chạy default tests (nhanh)
npx playwright test --project=chromium-default

# Chỉ chạy multiuser tests
npx playwright test --project=chromium-multiuser